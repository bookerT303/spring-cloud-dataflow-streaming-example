server:
  port: 8082

spring:
  application:
    name: processor-app

  cloud:
    stream:
      bindings:
        output:
          destination: demo-processed
        input:
          destination: demo
